# CloudBase Dev API Deployment Evidence

Last updated: 2026-06-22

## Scope

This document records the verified `dev` deployment state for CloudBase environment `cloud1-d7gxdk8t43bd639c0`. Production resources and production data were not touched.

## Verified Inventory

CloudBase MCP status:

- Auth: `READY`
- Bound env: `cloud1-d7gxdk8t43bd639c0`
- Alias: `cloud1`
- Region: `ap-shanghai`
- Env status: `NORMAL`
- Runtime backend: NoSQL
- Env info RequestId: `5cf43717-1255-46a7-b5f3-c507c9bac0fa`

NoSQL collections from live MCP query:

| Collection | Count | Index count |
| --- | ---: | ---: |
| `configs` | 0 | 2 |
| `file_assets` | 0 | 2 |
| `operation_logs` | 0 | 2 |
| `places` | 0 | 7 |
| `users` | 0 | 2 |

`places` indexes verified live:

- `_id_`
- `_openid_1`
- `idx_places_community_status`: `community_id` asc + `status` asc
- `idx_places_category_level_1`: `category_level_1` asc
- `idx_places_category_level_2`: `category_level_2` asc
- `idx_places_is_recommended`: `is_recommended` asc
- `idx_places_recommended_rank`: `recommended_rank` asc

Static hosting:

- Status: `online`
- Domain: `cloud1-d7gxdk8t43bd639c0-1441004938.tcloudbaseapp.com`
- Bucket: `8d38-static-cloud1-d7gxdk8t43bd639c0-1441004938`
- Status RequestId: `66726cf9-d3f4-460f-82ed-38ecb43c5669`

Log service:

- CLS aggregate log service status: not enabled.
- Function execution logs are available through `queryFunctions(action="listFunctionLogs")`.

## Function Deployment

`community-map-api` was replaced from the old Event placeholder with a CloudBase HTTP function.

Current function metadata:

- Function name: `community-map-api`
- Function ID: `lam-igyxxjph`
- Type: `HTTP`
- Runtime: `Nodejs18.15`
- Timeout: `30`
- Status: `Active`
- Available status: `Available`
- Code deploy mode: `code`
- Latest code update RequestId: `70783676-7336-48f1-a9cd-926b68e396cf`
- Current detail RequestId: `945459d3-2a50-4b59-92fc-cbd762fdef36`

Environment variables verified on the function:

- `API_PROVIDER=cloudbase`
- `CLOUDBASE_PROVIDER_MODE=live`
- `CLOUDBASE_ENV_ID=cloud1-d7gxdk8t43bd639c0`

Deployment notes:

- The previous Event placeholder was deleted in dev before recreation. Delete RequestId: `f773f3a2-3904-45b4-a6ff-5e21381db4c4`.
- HTTP function creation RequestId: `92e251e3-556d-4e1f-a0a1-54d10322cc6a`.
- The deployable package is generated by `pnpm --filter @community-map/api build:cloudbase-http`.
- The package bundles the TypeScript API, shared contracts, and runtime dependencies into self-contained `index.cjs`, ships `scf_bootstrap`, listens on port `9000`, and reuses `createApp()`.

Function permission boundary:

- `queryPermissions` still reports the function resource rule as `auth != null && auth.loginType != 'ANONYMOUS'`.
- The `/api` gateway access has `EnableAuth=false`, and public smoke requests through the gateway succeed.
- Permission query RequestId: `d275c9fd-eecc-4289-b312-ffefa0497436`.

## Gateway Route

CloudBase HTTP access for `community-map-api` is configured:

- Access path: `/api`
- Access URL: `https://cloud1-d7gxdk8t43bd639c0.service.tcloudbase.com/api`
- APIId: `083b66e0-c43a-4af4-864f-f3a297353828`
- Target: `community-map-api`
- EnableAuth: `false`
- PathTransmission: `2`
- Route query RequestId: `d3dc381e-8fbe-4926-b680-029664145484`

The API also accepts `/api/...` locally because the Koa app normalizes the `/api` prefix before route matching.

## Smoke Evidence

Smoke base URL:

```text
https://cloud1-d7gxdk8t43bd639c0.service.tcloudbase.com/api
```

Results from 2026-06-22:

| Endpoint | HTTP status | Body summary | Gateway request ID | App request ID |
| --- | ---: | --- | --- | --- |
| `GET /health` | 200 | `{ "ok": true }` | `bc257000-fcda-4822-a4e4-8f1c323ce523` | not enveloped |
| `GET /places?page=1&pageSize=5` | 200 | `items: [], total: 0` | `44e7e909-2894-4656-a5e1-3a8a44a8da7a` | `0b3d271f-d53b-4e43-84a7-081c955d3a01` |
| `GET /places/map-markers` | 200 | `data: []` | `59a9e3e9-9cff-4ff1-9ac7-fdcdd0fca4a3` | `25db96e0-f516-49f3-a38e-6a45da98b1f6` |

Matching function log evidence:

- Health log: `bc257000-fcda-4822-a4e4-8f1c323ce523`, duration `9ms`, `RetMsg: {"ok":true}`.
- Places list log: `44e7e909-2894-4656-a5e1-3a8a44a8da7a`, duration `186ms`, empty live result.
- Map markers log: `59a9e3e9-9cff-4ff1-9ac7-fdcdd0fca4a3`, duration `193ms`, empty live result.

## Live Provider Proof

The places smoke used live CloudBase configuration, not mock fallback:

- Function env vars are `API_PROVIDER=cloudbase`, `CLOUDBASE_PROVIDER_MODE=live`, and `CLOUDBASE_ENV_ID=cloud1-d7gxdk8t43bd639c0`.
- Live `places` collection query returned `total=0` with RequestId `9964fef3-1c4c-40ca-8b6f-b581b6a8fd33`.
- The HTTP smoke returned empty places list and empty map markers.
- The mock provider currently returns two places locally, so the empty CloudBase response is consistent with live NoSQL data rather than mock fallback.
- Detail smoke was not run because there is no published live place in the `places` collection.

## Remaining Work

- Seed or import minimum live places data before full places live acceptance.
- Verify one published detail endpoint after live data exists.
- Verify admin create/update against CloudBase live data.
- Verify gallery media temporary URL behavior with real CloudBase file IDs.
- Complete non-places live providers for events, discover, comments, announcements, notifications, and files.
- Decide final production auth/security rules before any production exposure.
