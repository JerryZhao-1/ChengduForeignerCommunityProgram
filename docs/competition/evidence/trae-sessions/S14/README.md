# S14 raw TRAE evidence placeholder

Do not infer or shorten the Session ID. From the current TRAE UI, save:

- one uncropped screenshot showing the full current Session ID and session title;
- the deployment/review conversation around the CloudBase build and the fail-closed decision;
- any TRAE terminal output that shows the exact command and real exit status;
- product screenshots only when they show the browser URL and observed state without credentials.

Paste the complete Session ID into the S14 row of `docs/competition/trae-evidence-log.md`. This folder currently contains no raw TRAE screenshot, so S14 remains `PENDING-TRAE-EVIDENCE`.
