# MCP Runbook

Use MCP queryFunctions(getFunctionDetail), queryPermissions, and listFunctionLogs to re-check live function state.

Expected evidence is recorded in `docs/cloudbase-dev-api-deployment.md`. Do not mutate CloudBase resources while supervising read-only inventory tasks.
