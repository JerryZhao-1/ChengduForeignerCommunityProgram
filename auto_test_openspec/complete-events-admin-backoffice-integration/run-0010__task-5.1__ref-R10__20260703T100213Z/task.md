# Validation Bundle: Task 5.1

- change-id: `complete-events-admin-backoffice-integration`
- run: `RUN #10`
- task-id: `5.1`
- ref-id: `R10`
- scope: `CLI`

## How To Run

Run `bash run.sh` on macOS/Linux or `run.bat` on Windows.

## Expected Results

The bundle runs documentation grep assertions. Expected result is exit code `0`; logs must show new endpoint docs, authorization notes, current envelope examples, mock actor headers, manual smoke expectations, and deferred registration export notes.

## Provenance

Assertions are derived from task ACCEPT and the updated docs.
