# CLI Assertions

Run `run.sh` or `run.bat`.

Required assertions:

- `docs/已实现API接口清单.md` lists `GET /admin/events` and `GET /admin/events/:id/registrations`.
- `docs/API接口使用手册.md` documents auth, request/response shape, common errors, mock actor header examples, manual Admin/Mobile smoke expectations, and export deferral.
