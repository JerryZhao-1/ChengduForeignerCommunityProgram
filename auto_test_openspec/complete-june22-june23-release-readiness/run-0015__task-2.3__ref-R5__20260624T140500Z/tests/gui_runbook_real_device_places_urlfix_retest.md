# GUI Runbook: Real-Device Places URL Fix Retest

Scan `outputs/preview-urlfix.png` with the same or another physical WeChat-capable device.

Record:

1. Device model, OS version, WeChat version, and exact test time.
2. Whether the map page still shows `Can't find variable: URL`.
3. Whether places list, places map, and a place detail page are reachable.
4. Tested place id, if visible in the detail URL, debug panel, or detail evidence.
5. Whether marker selection works.
6. Whether native navigation opens or shows an acceptable permission fallback.
7. Whether share opens the WeChat share sheet or records a platform limitation.
8. Screenshots of the map page after loading and the detail/navigation/share result.

Task `2.3` can be checked only after this retest evidence exists.

