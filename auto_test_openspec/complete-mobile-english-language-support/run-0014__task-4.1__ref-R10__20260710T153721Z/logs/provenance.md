# Evidence provenance

All JSON inputs are checked-in golden fixtures under `scripts/fixtures/bilingual-content-audit/`. The production-candidate-shaped sample exists to test the gate contract and is not represented as a real CloudBase export. Output timestamps and source query/collection/count metadata are preserved in each audit result.
