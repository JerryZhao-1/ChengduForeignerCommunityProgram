# GUI Evidence

- Mobile H5 URL: `http://127.0.0.1:5174/#/pages/places/index`
- Browser check: tapping `#service` from a place card showed active filter `标签 #service` and reduced visible cards to one result.
- Reset control remained visible as `清空筛选`.
- Initial stale Vite optimized dependency was found and fixed by excluding `@community-map/shared` from frontend `optimizeDeps`.

