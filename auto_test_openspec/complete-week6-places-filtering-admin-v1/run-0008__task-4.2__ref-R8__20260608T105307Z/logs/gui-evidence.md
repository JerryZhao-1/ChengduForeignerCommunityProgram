# GUI Evidence

- Admin H5 URL: `http://127.0.0.1:5173/places`
- Browser check confirmed the gallery manager is present, uses a file-name registration input, exposes `登记图集文件`, and does not expose a manual gallery URL input.
- CLI route tests cover file permission paths and place gallery attachment persistence.

